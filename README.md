# Game Score API

Backend REST API built with Node.js and Express to handle users and game scores.

## Features
- User login with JWT
- Save player scores
- Get leaderboard

## Endpoints
POST /api/auth/login  
POST /api/scores  
GET /api/scores  

## Tech Stack
Node.js, Express, JWT
